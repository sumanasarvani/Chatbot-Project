# 💬 Simple Chatbot (No Memory)

A lightweight Python chatbot that uses the **NVIDIA API** (via the `OpenAI` SDK) to generate responses.  
This version does **not** store previous messages — every prompt is treated as a new question.

---

## 🚀 Features

- ⚡ **Simple and fast** — responds instantly to each input  
- 💬 **Stateless** — no memory; each reply is independent  
- 🧠 **NVIDIA API powered** — connects through the `OpenAI` client using NVIDIA’s endpoint  
- 🔐 **Environment variable support** — keeps your API key safe with `.env` file  
